# 프론트엔드 환경변수 예시 — 복사해서 .env 로 사용하세요.
# 개발 시에는 비워두면 Vite proxy(/api → localhost:4000)를 사용합니다.
VITE_API_BASE_URL=/api

# (선택) 구글 로그인 사용 시: 구글 클라우드 콘솔에서 발급한 OAuth 클라이언트 ID.
# 비워두면 "구글로 로그인" 버튼은 표시되지 않습니다(이메일 회원가입은 그대로 동작).
# VITE_GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
